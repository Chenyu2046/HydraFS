---
artifact: prd
version: "1.1"
created: 2026-05-13
status: draft
---

# PRD：分布式双链知识云存储系统

## Overview

### Problem Statement

传统云盘解决的是“把文件存进去”和“把文件取出来”，但对目标用户真正有价值的往往是文件里的知识，而不是文件本身。随着学习资料、项目文档、论文、代码片段、Markdown 笔记持续积累，用户会逐渐遇到三个问题：

1. 文件越来越多，但仍然只能靠文件名、目录和记忆去找。
2. 文件之间缺乏内容关联，知识点散落在不同文件中，无法形成可探索结构。
3. 上传后的文件只是静态资产，不能自动形成摘要、标签、关联和来源回溯。

对本项目的目标用户来说，问题不是“没有地方存文件”，而是“存进去之后很难重新利用”。如果系统只能做到上传、下载、分享，那么它仍然是一个传统云盘；如果系统能在不改变现有分布式存储底座的前提下，把文件升级为可理解、可检索、可回溯、可关联的知识资产，项目的产品价值和面试辨识度都会明显提升。

### Solution Summary

在现有云存储底座上增加一层轻量知识层。用户上传文件后，系统在不影响原有上传下载体验的前提下，异步完成内容理解和知识组织，形成以下闭环：

```text
上传文件
  → 保存原始文件
  → 生成 AI 文件卡片（摘要、标签、描述）
  → 生成文件级 Wiki 页面
  → 识别显式双链
  → 展示反向链接
  → 支持可解释的搜索结果
  → 帮助用户从“找文件”升级为“找知识”
```

本期 MVP 不追求一次性做成完整知识图谱平台，而是先把主价值做实：`云盘底座 + AI 文件卡片 + 文件级 Wiki + 显式双链与反向链接 + 可解释搜索`。轻量关系图、Wiki 分享、人工编辑等能力保留为后续增强项，不作为 MVP 完成的前提。

### Target Users

- **主要用户：** 学生、程序员、研究人员、独立开发者，拥有较多 PDF、Markdown、TXT、代码文件和项目资料，需要长期沉淀和复用知识。
- **次要用户：** 轻知识管理需求用户，希望把个人云盘升级为“可搜索、可总结、可关联”的资料中心。
- **管理员：** 需要查看系统基础运行状态和文件解析状态的维护者。

## Goals & Success Metrics

### Goals

1. 在不重构现有存储底座的前提下，让系统从传统云盘升级为知识型云存储。
2. 让用户在上传文件后，能够自动获得摘要、标签、Wiki 入口和可追溯知识视图。
3. 让搜索从“按文件名找文件”升级为“按语义找内容，并知道为什么命中”。
4. 保持原有上传、秒传、分享、下载链路可用，新增能力不阻塞基础文件服务。

### Success Metrics

| Metric | Current Baseline | Target | Timeline |
|--------|-----------------|--------|----------|
| 支持类型文件上传后生成文件卡片或 Wiki 的比例 | 0% | 80% 以上 | MVP 交付时 |
| 语义搜索 Top10 首屏返回时间 | 无 | 1s 内 | MVP 交付时 |
| 搜索结果包含命中原因的比例 | 0% | Top10 结果 100% 带命中说明 | MVP 交付时 |
| 上传链路被 AI 处理阻塞的比例 | 存在潜在风险 | 0%，必须异步 | MVP 交付时 |
| 样例 Markdown 文件的显式双链可成功展示反向链接的比例 | 0% | 样例集 100% 可回查 | MVP 演示时 |

### Non-Goals

- 不在 MVP 内实现实时多人协同编辑。
- 不在 MVP 内实现复杂组织权限、多租户、部门级 ACL。
- 不在 MVP 内实现完整在线 Office 编辑能力。
- 不在 MVP 内构建复杂多跳图推理平台。
- 不在 MVP 内引入新的重型产品层以替代当前云盘主流程。

## User Stories

| ID | User Story | Priority |
|----|-----------|----------|
| US-1 | 作为用户，我希望上传 Markdown、PDF、TXT 或代码文件后，系统能自动生成摘要和标签，便于我快速理解文件内容 | P0 |
| US-2 | 作为用户，我希望搜索“分布式文件存储”“上周那份接口文档”这类自然语言时，系统能返回相关文件结果并告诉我为什么命中 | P0 |
| US-3 | 作为用户，我希望打开某个文件后，不只看到下载地址，还能看到它的知识视图、关键点和相关文件 | P0 |
| US-4 | 作为用户，我希望在 Markdown 中出现 `[[概念]]` 时，系统能识别成双链关系，并支持查看反向引用 | P0 |
| US-5 | 作为用户，我希望看到和当前文件最相关的其他文件，减少重复搜索和手工整理 | P1 |
| US-6 | 作为用户，我希望后续可以通过一个轻量关系图入口快速理解某个主题和哪些文件相关 | P2 |
| US-7 | 作为管理员，我希望后续能看到更完整的任务总览与失败重试入口 | P2 |

## Scope

### In Scope

- 保留并复用现有登录注册、上传下载、秒传、分片上传、文件列表、分享能力。
- 对支持的文本类文件生成 AI 文件卡片，包括摘要、标签、可搜索描述。
- 为可解析文件生成文件级 Wiki 页面，作为文件的知识视图。
- 支持显式双链识别，例如 Markdown 中的 `[[FastDFS]]`。
- 支持反向链接展示，即某个概念或页面被哪些文件引用。
- 支持关键词搜索与语义搜索并存。
- 支持展示相关文件与搜索命中原因。
- 支持文件级解析状态展示，确保 AI 解析不阻塞上传主链路。
- 明确知识层默认按用户隔离，物理文件可复用，但用户可见 Wiki、关系和检索视图默认属于当前用户空间。

### Out of Scope

- 实时协同编辑、评论、版本冲突解决。
- 企业级组织权限、多租户隔离、部门空间管理。
- 在线 Office 文档编辑器。
- 复杂 OCR 平台和大规模多模态解析流水线。
- 多跳图推理、Agent 编排、自动决策类知识系统。
- 独立任务中心页面。
- 轻量关系图页面。
- Wiki 页面独立分享。

### Future Considerations

- **手动编辑 Wiki 页面：** MVP 先自动生成和只读展示，后续再支持人工修订。
- **手动添加或删除双链：** MVP 先支持系统识别，后续再开放用户精调。
- **更丰富的文件类型支持：** 如 docx、图片 OCR、音视频转写，后续按价值扩展。
- **Wiki 页面分享：** 先保留原始文件分享，后续再考虑分享知识视图而不是只分享文件。
- **轻量关系图：** 当前不作为 MVP 必做项，后续可作为演示增强层增加。
- **独立任务中心：** 当前只要求文件级状态可见，完整任务总览与批量重试放到后续阶段。

## Solution Design

### Functional Requirements

#### 1. 文件上传与知识任务触发

- FR-1：用户上传文件后，系统必须继续沿用现有云盘主链路保存文件。
- FR-2：上传成功后，系统应自动创建异步解析任务，而不是在上传接口内同步完成 AI 处理。
- FR-3：对于不支持解析的文件类型，系统应保留文件存储能力，但将解析状态标记为 `skipped` 或等价状态。
- FR-4：当文件已命中秒传时，系统仍应能够为当前用户补齐知识层记录，而不是因为物理文件已存在就跳过知识入口。
- FR-5：系统可以复用同一物理文件的全局解析缓存，但用户可见的 Wiki、双链、反向链接和检索视图默认按用户隔离生成与展示。

#### 2. AI 文件理解

- FR-6：系统应支持对 `txt`、`md`、`pdf`、常见代码文本文件进行内容提取和理解。
- FR-7：系统应为每个可解析文件生成至少一份摘要、一组标签和一份可搜索描述。
- FR-8：摘要应控制在可快速浏览的长度范围内，首版建议为 100 至 300 字。
- FR-9：标签首版控制在 3 至 5 个以内，避免输出失控。
- FR-10：系统应在用户可见位置展示文件解析状态，包括待处理、处理中、成功、失败、跳过。
- FR-11：当 AI 能力不可用时，系统应保证文件上传和基础云盘功能不受影响。

#### 3. Wiki 页面

- FR-12：每个可解析文件应生成一个 Wiki 页面，作为该文件的知识视图。
- FR-13：Wiki 页面至少包含标题、来源文件、摘要、标签、关键内容块或正文大纲、相关文件入口。
- FR-14：用户应能从文件详情页跳转到对应 Wiki 页面。
- FR-15：Wiki 页面应支持回溯到原始文件，避免知识视图与原始资料脱节。
- FR-16：MVP 中 Wiki 页面只支持系统自动生成与查看，不要求用户手动编辑。

#### 4. 双链与反向链接

- FR-17：系统应识别 Markdown 等文本内容中的显式双链标记，如 `[[概念]]`。
- FR-18：当系统识别到显式双链概念时，应建立当前文件或 Wiki 到概念节点的关系。
- FR-19：系统应展示反向链接，即当前概念、页面或主题被哪些文件引用。
- FR-20：MVP 只要求显式双链与反向链接闭环可用；隐式关联如未达到稳定标准，可降级为“相关文件推荐”而非正式双链。

#### 5. 搜索与相关推荐

- FR-21：系统必须保留原有文件名、列表等基础检索方式。
- FR-22：系统应支持自然语言语义搜索，并返回相关文件结果。
- FR-23：搜索结果应展示命中原因，首版至少包含以下三类之一：摘要片段命中、标签命中、语义相似度说明。
- FR-24：文件详情页或 Wiki 页面应展示“相关文件”列表，首版返回 3 个以内结果，并为每个结果提供关联原因。
- FR-25：MVP 中搜索结果主返回对象为文件结果，必要时可以附带其对应 Wiki 入口，但不要求首版同时构建完整双结果体系。

#### 6. 分享与外部访问

- FR-26：系统应继续支持原始文件分享能力。
- FR-27：MVP 阶段不要求对 Wiki 页面提供独立分享能力。

#### 7. 任务与可观测性

- FR-28：系统应提供解析状态查询能力，至少支持文件级展示当前 AI 处理进度。
- FR-29：系统应记录 AI 解析失败原因，便于管理员排查和用户重试。
- FR-30：MVP 阶段不要求独立任务中心，但需为后续扩展预留状态字段和查询入口。

### User Experience

#### 核心体验目标

用户感知到的产品体验应当是：

1. 上传文件依旧像普通云盘一样直接。
2. 文件上传后，会异步生成摘要、标签和 Wiki 入口。
3. 用户既可以通过自然语言搜索找到目标，也可以通过 Wiki 和反向链接继续探索。
4. 打开一个文件时，不只看到文件本身，还能看到“它讲了什么、和什么相关、谁在引用它”。

在 MVP 中，“异步生成”定义为：对支持类型的小中型文件，系统在上传成功后进入后台处理，并在可接受的演示时间内完成文件卡片或 Wiki 生成；若处理失败，必须明确展示失败状态，而不是静默无结果。

#### MVP 关键页面

- 登录 / 注册页
- 文件首页
- 文件详情页
- Wiki 页面
- 搜索页
- 分享访问页

#### 关键用户路径

**路径 1：上传后形成知识视图**

```text
用户上传文件
  → 文件进入云盘列表
  → 页面显示解析中
  → 解析成功后显示摘要、标签、Wiki 入口
  → 用户点击进入 Wiki 页面查看知识视图
```

**路径 2：从搜索进入知识探索**

```text
用户输入自然语言查询
  → 返回文件结果
  → 展示命中摘要与命中原因
  → 用户点击结果进入文件详情或 Wiki
  → 用户继续查看相关文件与反向链接
```

**路径 3：从双链进入上下文**

```text
用户打开某个 Wiki
  → 查看其中的概念链接
  → 点击概念
  → 查看引用该概念的其他文件
  → 进入相关文件继续探索
```

### Edge Cases

| Scenario | Expected Behavior |
|----------|------------------|
| 上传文件成功，但 AI 服务不可用 | 文件仍上传成功，解析状态显示失败或待重试，不影响基础云盘功能 |
| 文件命中秒传，但当前用户无知识层记录 | 补建当前用户的知识记录，不重复上传物理文件 |
| 文件类型不支持解析 | 文件正常存储，状态标记为跳过，前端明确提示 |
| 语义搜索依赖不可用 | 系统降级到关键词或文件名搜索 |
| 显式双链引用的概念尚不存在 | 自动创建概念占位并建立关系 |
| 文件删除后存在知识记录或关系 | 对应知识视图和关系应同步失效、软删除或重建，避免脏展示 |
| 同一文件被多个用户引用 | 物理文件复用不受影响，可复用全局解析缓存，但用户可见知识视图默认彼此隔离 |

## Technical Considerations

### Constraints

- 必须保留现有分布式云存储底座，不因知识层建设改变基础文件存储主路径。
- 新增知识层能力不能阻塞上传、下载、秒传、分享等基础主链路。
- 新能力应优先复用现有系统已具备的存储、检索和缓存能力，避免为了 MVP 进行大规模重构。
- 所有知识层能力必须以“用户可见、可解释、可回溯”为前提。

### Integration Points

- **基础云盘能力：** 上传、下载、秒传、分片上传、分享继续作为产品底座。
- **AI 理解能力：** 负责文件摘要、标签、可搜索描述以及 Wiki 生成。
- **搜索能力：** 负责关键词检索、语义检索以及命中解释。
- **知识视图能力：** 负责文件级 Wiki、显式双链和反向链接展示。

### Data Requirements

- 系统需要补充文件知识视图、关系、任务状态等元数据。
- 用户可见的知识记录必须默认按用户隔离，避免不同用户之间的数据串联。
- 搜索和关系展示需要可追溯到原始文件，避免形成不可解释的黑盒结果。
- 任务状态和失败原因需要持久化，支持重试与运营排查。

## Dependencies & Risks

### Dependencies

| Dependency | Owner | Status | Impact if Delayed |
|------------|-------|--------|-------------------|
| 现有上传、秒传、分片上传能力稳定可用 | 当前项目 | 已具备 | 若不稳定，知识层无法建立在可靠底座上 |
| 现有 AI 描述与向量检索能力可复用 | 当前项目 | 已具备基础 | 若复用受限，需缩小 MVP 到摘要和标签 |
| 前端新增 Wiki 页面与搜索解释展示 | 当前项目 | 待实现 | 若前端落地慢，产品亮点无法完整演示 |
| 文件级解析状态与失败反馈 | 当前项目 | 待增强 | 若缺失，会降低系统可控性与演示完整度 |

### Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| PRD 范围过大，演变成完整知识平台 | High | High | 锁定 MVP 只做知识卡片、Wiki、显式双链、反向链接、搜索解释 |
| AI 解析链路影响上传体验 | Medium | High | 强制异步化，上传成功与 AI 结果解耦 |
| 搜索结果可解释性弱，用户不信任 | Medium | Medium | 展示命中原因、摘要片段和相关来源 |
| 用户私有知识视图与物理文件复用规则不清 | Medium | High | 明确“物理文件可复用，用户视图默认私有，必要时复用全局解析缓存” |
| 多用户文件复用下知识记录串库 | Medium | High | 明确知识层记录按用户隔离，复用物理文件但不混用用户视图 |
| 文档型需求漂移为技术实现清单 | Medium | Medium | PRD 只定产品边界，技术方案另行拆分 |

## Timeline & Milestones

| Milestone | Description | Target Date |
|-----------|-------------|-------------|
| M1：底座确认 | 确认现有云盘、秒传、分片上传、AI 搜索作为底座可复用能力 | 2026-05-16 |
| M2：知识卡片 MVP | 完成摘要、标签、相关文件、搜索结果解释 | 2026-05-23 |
| M3：Wiki MVP | 完成 Wiki 页面、来源回溯、基础状态展示 | 2026-05-30 |
| M4：双链与反向链接 | 完成显式双链识别与反向链接展示 | 2026-06-06 |
| M5：演示闭环 | 完成“上传 → 解析 → Wiki → 搜索 → 反向链接”的完整演示路径 | 2026-06-13 |

## Open Questions

- [ ] 对于同一 md5 被多个用户复用的场景，是否在后续版本开放“共享知识视图”的可选模式？ Owner: Product
- [ ] 轻量关系图是否在 MVP 之后作为第一批增强能力进入下一阶段？ Owner: Product

## Appendix

### Related Documents

- 项目总览：[README.md](README.md)
- AI 检索现状：[ai_search.md](ai_search.md)
- 分片上传现状：[chunked_upload.md](chunked_upload.md)

### Revision History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.1 | 2026-05-13 | Codex | Tightened MVP scope, clarified user-private knowledge rules, and reduced implementation-heavy PRD content |
| 1.0 | 2026-05-13 | Codex | Initial draft |
